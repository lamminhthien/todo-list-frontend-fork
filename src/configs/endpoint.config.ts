const PREFIX = 'api';

export const API_ENDPOINTS = {
  AUTH: `${PREFIX}/auth`,
  USER: `${PREFIX}/users`,
  TASK: `${PREFIX}/tasks`,
  LIST: `${PREFIX}/todolists`,
  FAVORITE: `${PREFIX}/favorites`
};
